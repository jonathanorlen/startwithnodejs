// const fs = require('fs');
// const chalk = require('chalk');
// const until = require('./until');

// fs.writeFileSync('note.txt', until.name);
// fs.appendFileSync('note.txt', until.sum(1,2));

// console.log(chalk.red.bold(until.name))

// OS Module
// const os = require("os");
// console.log(os.cpus())